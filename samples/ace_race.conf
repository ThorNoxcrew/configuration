game = ACE_RACE
namespace = ISLAND
samples = [
  {
    modifiers = {
      "maps" = {
        pool = [
          { name = "clouds" }
          // TODO: Fix this map { name = "pythons_crypt" }
          { name = "space" }
        ]
      }
    }
  }
]
