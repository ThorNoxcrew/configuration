// Allows overriding various biome settings.
// Any values set here will override *all* biomes.

// Currently only active on lobby servers.

// Available values: fog, water, waterFog, sky, foliage, grass.

water = "#43D5EE"
