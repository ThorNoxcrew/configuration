phaseDurations = {
    preGame = 0s // The preparation phase is effectively the pre-game here.
    preparationPhase = 30s // Kit selection phase (once before game on island, once per round on event)
    countdownPhase = 5s
    battlePhase = 60s
    postRound = 10s
    postGame = 50s
}