phaseDurations = {
    preGame = 19s // Waiting Phase
    preparationPhase = 30s // Kit selection phase (once before game on island, once per round on event)
    countdownPhase = 5s
    battlePhase = 60s
    postRound = 10s
    postGame = 50s
}