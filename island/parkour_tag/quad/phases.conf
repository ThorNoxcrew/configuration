phaseDurations = {
    preGame = 20s // Waiting Phase
    preRound = 15s // Before Round Phase
    preparationPhase = 5s
    countdownPhase = 10s
    battlePhase = 60s
    postRound = 10s // Game Phase -> Intermission Phase
    intermission = 15s // Intermission Phase
    postGame = 50s // End Phase
}
