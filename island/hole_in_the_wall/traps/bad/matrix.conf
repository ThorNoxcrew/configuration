storm = {
  type = FIREBALL
  amount = 15
  rate = 2
  variance = 0.2s
  projectileSpeed = 0.005
  knockbackStrength = 1.5
  knockbackHeight = 1.5
  # Delay before the fireballs are killed at the end of the trap's duration
  cleanupDelay = 3s
  # A projectile storm uses the edge of the platform offset by the following vector to spawn projectiles
  platformOffset = [3, 2, 3]
}
