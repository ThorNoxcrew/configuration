storm = {
  type = FIREBALL
  amount = 50
  rate = 5
  variance = 0.2
  projectileSpeed = 1.0
  knockbackStrength = 1.5
  knockbackHeight = 1.5
  # Delay in seconds before the fireballs are killed at the end of the trap's duration
  cleanupDelay = 3.0
  # A projectile storm uses the edge of the platform offset by the following vector to spawn projectiles
  platformOffset = [3, 2, 3]
}
