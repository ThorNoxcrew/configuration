blockFall = {
  type = SAND
  amount = 30
  rate = 5
  variance = 0.2
  # Delay in seconds before the sand is killed at the end of the trap's duration
  cleanupDelay = 3.0
  platformOffset = [0, 20, 0]
}
