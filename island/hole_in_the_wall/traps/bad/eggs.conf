storm = {
  type = EGG
  amount = 30
  rate = 5
  variance = 0.2
  # Delay in seconds before the chickens are killed at the end of the trap's duration
  cleanupDelay = 3.0
  # A projectile storm uses the edge of the platform offset by the following vector to spawn projectiles
  platformOffset = [2, 2, 2]
}
