storm = {
  type = EGG
  amount = 20
  rate = 5
  variance = 0.2s
  # Delay before the eggs are killed at the end of the trap's duration
  cleanupDelay = 3s
  # A projectile storm uses the edge of the platform offset by the following vector to spawn projectiles
  platformOffset = [2, 4, 2]
  knockbackHeight = 1.2
  knockbackStrength = 2
}
