// The ID of the item to use.
itemId = "mcc:island_cosmetics.accessory_special.hitw_sumo_boxing_glove"

// The duration of the trap (how long until the item is removed).
duration = 10s

// Number of uses (how many successful punches until it is removed).
uses = 1

// Knockback strength (if uncommented, the strength of the knockback effect on the item, 0 is Level 1).
//knockback = 0
