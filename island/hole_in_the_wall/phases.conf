phaseDurations = {
    preGame = 25s // Waiting Phase
    preRound = 20s // Intermission Phase
    inRound = 3m // Game Phase
    postRound = 5s // Game Phase -> Intermission Phase
    postGame = 45s // End Phase
}
