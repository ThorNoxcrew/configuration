stages:
  - upload
  - deploy

upload_consul:
  stage: upload
  image: python:3-alpine
  script: "python .ci/fs2consulkv.py --fs-kv-path . --consul-url $CONSUL_URL --consul-acl-token $CONSUL_ACL_TOKEN --consul-kv-root configuration/$CI_COMMIT_BRANCH --skip-prompt"
