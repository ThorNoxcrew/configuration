# The current theme of the event.
theme = NONE

# The current mode of the event
# DEBUG = All modules are enabled
# TEST = Only testing and event modules are enabled
# LIVE = Only event modules are enabled
mode = DEBUG

# Below you can enable various modules to be enabled either during the live event or during testing.
# Whether these are enabled depends on the current mode. DEBUG mode enables all modules (even those not
# listed below), TEST enables the event and testing modules, LIVE enables only the event modules.ø
eventModules = [
    
]
testingModules = [

]

# The modules below are never used, not even in DEBUG mode.
disabledModules = [
    custom_build_mart_breaking_speeds
    golden_chicken
]