# include required("../spectator_equipment.conf")

# Add the riptide trident but keep the rest of the normal equipment
spectatorEquipment += {
	    material = TRIDENT
        nbt = [
            custom_riptide_trident_equipment
        ]
	}